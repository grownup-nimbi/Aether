# Aether
Aether is a read-only Base utility that helps you confirm network context, inspect addresses, and validate testnet deployments on Base Sepolia. It is intentionally conservative: it only uses read methods (RPC calls and eth_call) and never sends transactions
